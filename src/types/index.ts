export interface Adopter {
  name: string;
  logo: string;
  url: string;
}

export interface Supporter {
  name: string;
  role: string;
  company: string;
  image: string;
  quote: string;
}

export interface BrandAsset {
  name: string;
  description: string;
  downloadUrl: string;
}